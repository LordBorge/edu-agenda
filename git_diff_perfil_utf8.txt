diff --git a/src/screens/Perfil/index.tsx b/src/screens/Perfil/index.tsx
index cc28a71..cb0feb8 100644
--- a/src/screens/Perfil/index.tsx
+++ b/src/screens/Perfil/index.tsx
@@ -1,23 +1,46 @@
-import React, { useCallback, useState } from 'react';
+import React, { useCallback, useEffect, useState } from 'react';
 import {
   View, Text, ScrollView, StyleSheet, TouchableOpacity,
-  TextInput, StatusBar, Alert,
+  TextInput, StatusBar, KeyboardAvoidingView, Platform,
 } from 'react-native';
 import { SafeAreaView } from 'react-native-safe-area-context';
 import { useFocusEffect } from '@react-navigation/native';
 import {
   getProfessionalProfile,
   getScheduleSettingsForPeriod,
+  markInitialScheduleSetupComplete,
   updateProfessionalProfile,
   updateScheduleSettings,
 } from '../../database/queries';
 import { SchedulePeriod, ScheduleSettings, ThemePreference } from '../../types';
-import { generateScheduleSlots, isCompleteTime, ScheduleSlot } from '../../utils/time';
+import { generateScheduleSlots, isCompleteTime, minutesToTime, ScheduleSlot, timeToMinutes } from '../../utils/time';
 import { useAppTheme } from '../../theme';
 import { TimeInput } from '../../components/TimeInput';
+import { ConfirmDialog, type ConfirmDialogVariant } from '../../components/ConfirmDialog';
 
 type SettingsForm = Omit<ScheduleSettings, 'id'>;
 type ProfileForm = { name: string; subjects: string; work_periods: string; theme_preference: ThemePreference };
+type PerfilScreenProps = {
+  setupLocked?: boolean;
+  showSetupReminder?: boolean;
+  onSetupComplete?: () => void;
+};
+type ConfirmDialogState = {
+  visible: boolean;
+  title: string;
+  message: string;
+  confirmLabel?: string;
+  cancelLabel?: string | null;
+  variant?: ConfirmDialogVariant;
+  onConfirm: () => void | Promise<void>;
+};
+
+const EMPTY_CONFIRM_DIALOG: ConfirmDialogState = {
+  visible: false,
+  title: '',
+  message: '',
+  onConfirm: () => undefined,
+};
 
 const PERIODS: Array<{ key: SchedulePeriod; label: string }> = [
   { key: 'integral', label: 'Integral' },
@@ -29,6 +52,10 @@ const DEFAULT_FORM: SettingsForm = {
   period: 'integral',
   start_time: '07:30',
   end_time: '17:00',
+  morning_start_time: '07:30',
+  morning_end_time: '12:00',
+  afternoon_start_time: '13:00',
+  afternoon_end_time: '17:00',
   lesson_duration: 48,
   break_duration: 20,
   break_after_lesson: 2,
@@ -56,6 +83,10 @@ function buildSlots(form: SettingsForm): ScheduleSlot[] {
     period: form.period,
     startTime: form.start_time,
     endTime: form.end_time,
+    morningStartTime: form.morning_start_time,
+    morningEndTime: form.morning_end_time,
+    afternoonStartTime: form.afternoon_start_time,
+    afternoonEndTime: form.afternoon_end_time,
     lessonDuration: form.lesson_duration,
     breakDuration: form.break_duration,
     breakAfterLesson: form.break_after_lesson,
@@ -71,6 +102,10 @@ function formFromSettings(settings: ScheduleSettings): SettingsForm {
     period: settings.period,
     start_time: settings.start_time,
     end_time: settings.end_time,
+    morning_start_time: settings.morning_start_time,
+    morning_end_time: settings.morning_end_time,
+    afternoon_start_time: settings.afternoon_start_time,
+    afternoon_end_time: settings.afternoon_end_time,
     lesson_duration: settings.lesson_duration,
     break_duration: settings.break_duration,
     break_after_lesson: settings.break_after_lesson,
@@ -95,12 +130,57 @@ function normalizeWorkPeriod(workPeriods: string): SchedulePeriod {
   return periods[0] ?? 'integral';
 }
 
-export function PerfilScreen() {
+export function PerfilScreen({
+  setupLocked = false,
+  showSetupReminder = false,
+  onSetupComplete,
+}: PerfilScreenProps) {
   const { colors, setPreference } = useAppTheme();
   const [form, setForm] = useState<SettingsForm>(DEFAULT_FORM);
   const [profileForm, setProfileForm] = useState<ProfileForm>(DEFAULT_PROFILE);
   const [slots, setSlots] = useState<ScheduleSlot[]>(() => buildSlots(DEFAULT_FORM));
   const [saved, setSaved] = useState(false);
+  const [setupReminderShown, setSetupReminderShown] = useState(false);
+  const [confirmDialog, setConfirmDialog] = useState<ConfirmDialogState>(EMPTY_CONFIRM_DIALOG);
+
+  const closeConfirmDialog = () => setConfirmDialog(EMPTY_CONFIRM_DIALOG);
+
+  const confirmAndClose = async () => {
+    const action = confirmDialog.onConfirm;
+    closeConfirmDialog();
+    await action();
+  };
+
+  const showInfoDialog = (title: string, message: string) => {
+    setConfirmDialog({
+      visible: true,
+      title,
+      message,
+      confirmLabel: 'OK',
+      cancelLabel: null,
+      variant: 'info',
+      onConfirm: () => undefined,
+    });
+  };
+
+  useEffect(() => {
+    if (!showSetupReminder || setupReminderShown) return undefined;
+
+    const timer = setTimeout(() => {
+      setSetupReminderShown(true);
+      setConfirmDialog({
+        visible: true,
+        title: 'Configure seus hor├írios',
+        message: 'Defina o hor├írio de in├¡cio e t├®rmino das aulas, dura├º├úo dos per├¡odos, intervalos e almo├ºo para que sua agenda fique correta.',
+        confirmLabel: 'Entendi',
+        cancelLabel: null,
+        variant: 'info',
+        onConfirm: () => undefined,
+      });
+    }, 450);
+
+    return () => clearTimeout(timer);
+  }, [setupReminderShown, showSetupReminder]);
 
   const commitForm = (next: SettingsForm) => {
     setForm(next);
@@ -123,6 +203,7 @@ export function PerfilScreen() {
     setSlots(buildSlots(next));
   }, []);
 
+
   useFocusEffect(useCallback(() => { load(); }, [load]));
 
   const handlePeriodChange = async (period: SchedulePeriod) => {
@@ -132,7 +213,8 @@ export function PerfilScreen() {
   };
 
   const updateField = (key: keyof SettingsForm, value: string | number) => {
-    commitForm({ ...form, [key]: value } as SettingsForm);
+    const next = { ...form, [key]: value } as SettingsForm;
+    commitForm(next);
   };
 
   const updateNumberField = (
@@ -145,20 +227,89 @@ export function PerfilScreen() {
   };
 
   const handleSave = async () => {
+    if (!profileForm.name.trim()) {
+      showInfoDialog('Aten├º├úo', 'Informe seu nome.');
+      return;
+    }
+
+    if (!profileForm.subjects.trim()) {
+      showInfoDialog('Aten├º├úo', 'Informe pelo menos um componente curricular.');
+      return;
+    }
+
+    const settingsToSave: SettingsForm = {
+      ...form,
+    };
+
+    if (form.period === 'integral') {
+      settingsToSave.morning_start_time = form.start_time;
+      settingsToSave.morning_end_time = form.lunch_start;
+      const lunchStartMin = timeToMinutes(form.lunch_start);
+      const lunchEndMin = lunchStartMin + form.lunch_duration;
+      settingsToSave.afternoon_start_time = minutesToTime(lunchEndMin);
+      settingsToSave.afternoon_end_time = form.end_time;
+    } else if (form.period === 'manha') {
+      settingsToSave.morning_start_time = form.start_time;
+      settingsToSave.morning_end_time = form.end_time;
+    } else {
+      settingsToSave.afternoon_start_time = form.start_time;
+      settingsToSave.afternoon_end_time = form.end_time;
+    }
+
     const timeFields = [
-      { label: 'hor├írio de in├¡cio do dia', value: form.start_time },
-      { label: 'hor├írio de t├®rmino do dia', value: form.end_time },
-      ...(form.period === 'integral' ? [{ label: 'hor├írio de in├¡cio do almo├ºo', value: form.lunch_start }] : []),
+      { label: 'hor├írio de in├¡cio', value: settingsToSave.start_time },
+      { label: 'hor├írio de t├®rmino', value: settingsToSave.end_time },
+      ...(settingsToSave.period === 'integral' ? [
+        { label: 'hor├írio de almo├ºo', value: settingsToSave.lunch_start },
+      ] : []),
     ];
     const invalidTime = timeFields.find(field => !isCompleteTime(field.value));
 
     if (invalidTime) {
-      Alert.alert('Aten├º├úo', `Informe o ${invalidTime.label} no formato HH:MM.`);
+      showInfoDialog('Aten├º├úo', `Informe o ${invalidTime.label} no formato HH:MM.`);
+      return;
+    }
+
+    const startMin = timeToMinutes(settingsToSave.start_time);
+    const endMin = timeToMinutes(settingsToSave.end_time);
+
+    if (startMin >= endMin) {
+      showInfoDialog('Aten├º├úo', 'O hor├írio de in├¡cio precisa ser menor que o hor├írio de t├®rmino.');
+      return;
+    }
+
+    if (settingsToSave.lesson_duration <= 0) {
+      showInfoDialog('Aten├º├úo', 'Informe uma dura├º├úo de aula maior que zero.');
+      return;
+    }
+
+    if (settingsToSave.period === 'integral') {
+      const lunchStartMin = timeToMinutes(settingsToSave.lunch_start);
+      const lunchEndMin = lunchStartMin + settingsToSave.lunch_duration;
+
+      if (lunchStartMin <= startMin) {
+        showInfoDialog('Aten├º├úo', 'O hor├írio do almo├ºo deve ser ap├│s o in├¡cio das aulas.');
+        return;
+      }
+
+      if (lunchEndMin >= endMin) {
+        showInfoDialog('Aten├º├úo', 'O hor├írio de t├®rmino do almo├ºo deve ser anterior ao t├®rmino do dia.');
+        return;
+      }
+
+      if (settingsToSave.lunch_duration <= 0) {
+        showInfoDialog('Aten├º├úo', 'Informe uma dura├º├úo de almo├ºo maior que zero.');
+        return;
+      }
+    }
+
+    if (buildSlots(settingsToSave).length === 0) {
+      showInfoDialog('Aten├º├úo', 'A configura├º├úo atual n├úo gerou nenhum hor├írio. Revise a dura├º├úo das aulas e os hor├írios de trabalho.');
       return;
     }
 
     await Promise.all([
-      updateScheduleSettings(form),
+      updateScheduleSettings(settingsToSave),
       updateProfessionalProfile({
         name: profileForm.name.trim(),
         subjects: profileForm.subjects.trim(),
@@ -166,9 +317,10 @@ export function PerfilScreen() {
         theme_preference: profileForm.theme_preference,
         onboarded: 1,
       }),
+      markInitialScheduleSetupComplete(),
     ]);
     const [persistedSettings, persistedProfile] = await Promise.all([
-      getScheduleSettingsForPeriod(form.period),
+      getScheduleSettingsForPeriod(settingsToSave.period),
       getProfessionalProfile(),
     ]);
     const persisted = formFromSettings(persistedSettings);
@@ -183,6 +335,7 @@ export function PerfilScreen() {
     setPreference(persistedProfile.theme_preference ?? 'system');
     setSaved(true);
     setTimeout(() => setSaved(false), 2000);
+    onSetupComplete?.();
   };
 
   const handleThemeChange = async (themePreference: ThemePreference) => {
@@ -196,7 +349,7 @@ export function PerfilScreen() {
       subjects: persistedProfile.subjects,
       work_periods: persistedProfile.work_periods,
       theme_preference: themePreference,
-      onboarded: persistedProfile.onboarded || 1,
+      onboarded: persistedProfile.onboarded,
     });
   };
 
@@ -213,28 +366,37 @@ export function PerfilScreen() {
         backgroundColor={colors.background}
         translucent={false}
       />
+      <KeyboardAvoidingView
+        style={styles.keyboard}
+        behavior={Platform.OS === 'ios' ? 'padding' : 'height'}
+      >
       <View style={[styles.header, { borderBottomColor: colors.border }]}>
         <Text style={[styles.headerTitle, { color: colors.text }]}>Configura├º├Áes</Text>
         <TouchableOpacity
           style={[styles.saveBtn, { backgroundColor: saved ? colors.secondary : colors.primary }]}
           onPress={handleSave}
         >
-          <Text style={styles.saveBtnText}>{saved ? 'Salvo' : 'Salvar'}</Text>
+          <Text style={styles.saveBtnText}>{saved ? 'Salvo' : setupLocked ? 'Concluir' : 'Salvar'}</Text>
         </TouchableOpacity>
       </View>
 
-      <ScrollView contentContainerStyle={styles.body} showsVerticalScrollIndicator={false}>
+      <ScrollView
+        contentContainerStyle={styles.body}
+        keyboardDismissMode="interactive"
+        keyboardShouldPersistTaps="handled"
+        showsVerticalScrollIndicator={false}
+      >
         <View style={[styles.profileCard, { backgroundColor: colors.surface }]}>
           <View style={[styles.profileAvatar, { backgroundColor: colors.primary }]}>
             <Text style={styles.profileAvatarText}>{initials(profileForm.name)}</Text>
           </View>
           <View style={{ flex: 1 }}>
-            <Text style={[styles.profileName, { color: colors.text }]}>{profileForm.name.trim() || 'Perfil profissional'}</Text>
+            <Text style={[styles.profileName, { color: colors.text }]}>{profileForm.name.trim() || 'Seus dados'}</Text>
             <Text style={[styles.profileSub, { color: colors.textMuted }]}>{profileForm.subjects.trim() || 'Componentes Curriculares n├úo informados'}</Text>
           </View>
         </View>
 
-        <Text style={[styles.sectionTitle, { color: colors.text }]}>Dados profissionais</Text>
+        <Text style={[styles.sectionTitle, { color: colors.text }]}>Seus dados</Text>
         <Text style={[styles.fieldLabel, { color: colors.textMuted }]}>Nome</Text>
         <TextInput
           style={[styles.input, { backgroundColor: colors.surface, color: colors.text, borderColor: colors.border }]}
@@ -243,7 +405,7 @@ export function PerfilScreen() {
             setSaved(false);
             setProfileForm(current => ({ ...current, name: value }));
           }}
-          placeholder="Ex: Ana Paula"
+          placeholder="Ex.: Ana Paula"
           autoCapitalize="words"
         />
 
@@ -255,7 +417,7 @@ export function PerfilScreen() {
             setSaved(false);
             setProfileForm(current => ({ ...current, subjects: value }));
           }}
-          placeholder="Ex: Ingl├¬s, Reda├º├úo, Projeto de Vida"
+          placeholder="Ex.: Ingl├¬s, Projeto de Vida, Reda├º├úo"
           multiline
         />
 
@@ -344,33 +506,12 @@ export function PerfilScreen() {
           ))}
         </View>
 
-        {isAfternoon ? (
+        {/* Intervalo da manh├ú (Integral) ou Intervalo (Manh├ú) */}
+        {(form.period === 'integral' || form.period === 'manha') && (
           <>
-            <Text style={[styles.sectionTitle, { color: colors.text }]}>Intervalo da tarde</Text>
-            <View style={styles.timeRow}>
-              <View style={styles.timeBox}>
-                <Text style={[styles.fieldLabel, fieldLabelTheme]}>Dura├º├úo (min)</Text>
-                <TextInput
-                  style={[styles.smallInput, inputTheme]}
-                  value={String(form.afternoon_break_duration)}
-                  onChangeText={value => updateNumberField('afternoon_break_duration', value)}
-                  keyboardType="number-pad"
-                />
-              </View>
-              <View style={styles.timeBox}>
-                <Text style={[styles.fieldLabel, fieldLabelTheme]}>Ap├│s qual aula</Text>
-                <TextInput
-                  style={[styles.smallInput, inputTheme]}
-                  value={String(form.afternoon_break_after_lesson)}
-                  onChangeText={value => updateNumberField('afternoon_break_after_lesson', value)}
-                  keyboardType="number-pad"
-                />
-              </View>
-            </View>
-          </>
-        ) : (
-          <>
-            <Text style={[styles.sectionTitle, { color: colors.text }]}>{isIntegral ? 'Intervalo da manh├ú' : 'Intervalo'}</Text>
+            <Text style={[styles.sectionTitle, { color: colors.text }]}>
+              {form.period === 'integral' ? 'Intervalo da manh├ú' : 'Intervalo'}
+            </Text>
             <View style={styles.timeRow}>
               <View style={styles.timeBox}>
                 <Text style={[styles.fieldLabel, fieldLabelTheme]}>Dura├º├úo (min)</Text>
@@ -394,14 +535,14 @@ export function PerfilScreen() {
           </>
         )}
 
-        {isIntegral && (
+        {form.period === 'integral' && (
           <>
             <Text style={[styles.sectionTitle, { color: colors.text }]}>Almo├ºo</Text>
             <View style={styles.timeRow}>
               <View style={styles.timeBox}>
-                <Text style={[styles.fieldLabel, fieldLabelTheme]}>In├¡cio</Text>
+                <Text style={[styles.fieldLabel, fieldLabelTheme]}>Hor├írio de sa├¡da</Text>
                 <TimeInput
-                  style={[styles.smallInput, inputTheme]}
+                  style={[styles.timeInput, inputTheme, { color: colors.primary, borderColor: colors.primary }]}
                   value={form.lunch_start}
                   onChangeText={value => updateField('lunch_start', value)}
                   placeholder="12:00"
@@ -417,8 +558,14 @@ export function PerfilScreen() {
                 />
               </View>
             </View>
+          </>
+        )}
 
-            <Text style={[styles.sectionTitle, { color: colors.text }]}>Intervalo da tarde</Text>
+        {(form.period === 'integral' || form.period === 'tarde') && (
+          <>
+            <Text style={[styles.sectionTitle, { color: colors.text }]}>
+              {form.period === 'integral' ? 'Intervalo da tarde' : 'Intervalo'}
+            </Text>
             <View style={styles.timeRow}>
               <View style={styles.timeBox}>
                 <Text style={[styles.fieldLabel, fieldLabelTheme]}>Dura├º├úo (min)</Text>
@@ -480,12 +627,25 @@ export function PerfilScreen() {
 
         <View style={{ height: 80 }} />
       </ScrollView>
+      </KeyboardAvoidingView>
+
+      <ConfirmDialog
+        visible={confirmDialog.visible}
+        title={confirmDialog.title}
+        message={confirmDialog.message}
+        confirmLabel={confirmDialog.confirmLabel}
+        cancelLabel={confirmDialog.cancelLabel}
+        variant={confirmDialog.variant}
+        onCancel={closeConfirmDialog}
+        onConfirm={confirmAndClose}
+      />
     </SafeAreaView>
   );
 }
 
 const styles = StyleSheet.create({
   safe: { flex: 1, backgroundColor: '#FAFAFA' },
+  keyboard: { flex: 1 },
   header: {
     flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between',
     paddingHorizontal: 20, paddingVertical: 14,
